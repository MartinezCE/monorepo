import { PageNotAvailable } from '@wimet/apps-shared';
import Layout from '../components/UI/Layout';

const IndexPage = () => (
  <Layout>
    <PageNotAvailable />
  </Layout>
);

export default IndexPage;
